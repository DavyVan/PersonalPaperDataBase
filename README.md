# PersonalPaperDataBase
PPDB is a paper notes management system to facilitate researchers efficiently take note and search among them.

# Contributing

This project is purely written in `TypeScript`, all the *.js files in `app/js/build/` folder will be ignored by git.